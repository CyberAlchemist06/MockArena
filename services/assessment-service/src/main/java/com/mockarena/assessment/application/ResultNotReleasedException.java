package com.mockarena.assessment.application;

/** Deliberately does not carry result data: scheduled and manual results remain opaque until released. */
public class ResultNotReleasedException extends RuntimeException { }
